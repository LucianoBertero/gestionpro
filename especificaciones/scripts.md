pnpm dev              # 🚀 Levanta API + Web (via Turborepo)
pnpm dev:api          # Solo backend (NestJS en :3000)
pnpm dev:web          # Solo frontend (Next.js en :3001)
pnpm db:studio        # Prisma Studio (explorar DB visualmente)
pnpm db:push          # Sincronizar schema con DB
pnpm db:generate      # Regenerar Prisma client
pnpm test:api         # Tests del backend (Jest 100%)
pnpm docker:up        # PostgreSQL + Redis locales (por si falla la nube)
